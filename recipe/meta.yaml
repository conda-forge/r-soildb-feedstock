{% set version = "2.3" %}

package:
  name: r-soildb
  version: {{ version|replace("-", "_") }}

source:
  fn: soilDB_{{ version }}.tar.gz
  url:
    - https://cran.r-project.org/src/contrib/soilDB_{{ version }}.tar.gz
    - https://cran.r-project.org/src/contrib/Archive/soilDB/soilDB_{{ version }}.tar.gz
  sha256: feffcd356bcd7943d0540d249eb91e48a00c63fad872f287c3ac8edbaefdeaf5

build:
  number: 0
  script: R CMD INSTALL --build .
  skip: True  # [win]
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  build:
    - r-base
    - r-hmisc
    - r-xml
    - r-aqp
    - r-plyr
    - r-raster
    - r-reshape2
    - r-sp
  run:
    - r-base
    - r-hmisc
    - r-xml
    - r-aqp
    - r-plyr
    - r-raster
    - r-reshape2
    - r-sp

test:
  commands:
    - $R -e "library('soilDB')"  # [not win]
    - "\"%R%\" -e \"library('soilDB')\""  # [win]

about:
  home: http://ncss-tech.github.io/AQP/
  license: GPL (>= 3)
  license_family: GPL3
  summary: A collection of functions for reading data from USDA-NCSS soil databases.

extra:
  recipe-maintainers:
    - ocefpaf
    - bgruening
    - jdblischak
    - rvalieris
